---
import { t } from "i18next";

type Props = React.HTMLProps<HTMLDivElement>;

const today = new Date();
const { className } = Astro.props;
---

<footer class={`mt-auto pt-12 mb-2 text-center text-gray-600 text-xs ${className ?? ""}`}>
  {today.getFullYear()}{t("site.copyright")}
</footer>
